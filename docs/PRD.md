# FlowGallery 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | FlowGallery（瀑布画廊） |
| 文档版本 | v1.0 |
| 状态 | 待评审 |
| 设计稿 | `flowgallery-android.html`（可交互原型） |
| 参考产品 | macOS FlowVision |
| 平台 | Android（手机） |

---

## 1. 产品概述

### 1.1 背景与问题

图包、画集、素材收藏爱好者通常将图片按主题整理在多个文件夹中（如 `Landscapes/`、`Portraits/`、`Architecture/`）。现有手机图库存在以下痛点：

1. **无法聚合多文件夹**：系统图库按相册聚合，但自定义文件夹层级深、分散，浏览不便。
2. **缺少瀑布流视图**：系统图库为等宽网格，无法突出不同比例图片的视觉效果；Pinterest 式瀑布流更利于快速扫图。
3. **图包预览效率低**：打开图包需逐级进入目录，无法"一次添加、持续浏览"。

macOS 端 FlowVision 通过"选择多个文件夹 → 瀑布流浏览"解决了桌面端问题，但移动端缺乏同类产品。

### 1.2 产品定位

**FlowGallery 是一款面向图包/画集收藏者的安卓瀑布流图库浏览器**，核心价值：

> 一次选择多个文件夹，以 Pinterest 式瀑布流聚合浏览全部图片，支持文件夹维度筛选、全屏沉浸查看。

### 1.3 设计稿范围

本文档以 `flowgallery-android.html` 交互原型为唯一视觉与交互基准。原型包含以下完整界面：

- 首页（Home）：瀑布流 + 文件夹标签栏 + 统计栏 + FAB
- 文件夹选择模态框（Folder Modal）
- 全屏查看器（Viewer）
- 搜索页（Search）
- 设置页（Settings）

---

## 2. 目标用户与使用场景

### 2.1 目标用户

| 用户群 | 特征 | 核心诉求 |
|---|---|---|
| 二次元画集收藏者 | 拥有大量"图包"（按角色/画师/主题分文件夹） | 快速扫图、多文件夹聚合 |
| 设计师/素材收集者 | 图片素材按项目分类存放 | 效率浏览、质量识别（HD/SD） |
| 摄影爱好者 | 按拍摄场景分文件夹 | 瀑布流浏览、收藏精选 |

### 2.2 典型使用场景

1. **图包聚合浏览**：用户添加 3 个图包文件夹，首页默认"All"聚合显示全部图片（含数量统计），按文件夹标签切换查看单个图包。
2. **快速扫图**：上下滑动瀑布流快速浏览，卡片左下角 HD/SD 徽章帮助识别质量，点击心形即时收藏。
3. **沉浸看图**：点击图片进入全屏查看器，双指缩放 1x–4x、滑动切换、底部缩略图条跳转。
4. **图库维护**：在设置页开关文件夹（临时隐藏）、添加新文件夹。

---

## 3. 竞品分析

| 维度 | FlowGallery | 系统相册 | Pinterest | macOS FlowVision |
|---|---|---|---|---|
| 多文件夹聚合 | ✅ 多选文件夹合并浏览 | ❌ 按相册隔离 | ❌ 按话题流 | ✅ |
| 瀑布流布局 | ✅ 双列最短列分配 | ❌ 等宽网格 | ✅ | ✅ |
| 文件夹级筛选 | ✅ 标签栏切换 | 部分 | ❌ | ✅ |
| 全屏沉浸查看 | ✅ 缩放+滑动+缩略图条 | ✅ | 部分 | ✅ |
| HD/SD 质量标识 | ✅ 徽章 | ❌ | ❌ | ❌ |
| 本地图包管理 | ✅ SAF 免权限 | 部分 | ❌ | ✅ |

**差异化结论**：FlowGallery = 系统相册的本地图库能力 + Pinterest 的瀑布流体验 + FlowVision 的多文件夹管理，并增加质量标识、收藏等移动端特性。

---

## 4. 信息架构

```
FlowGallery
├── 首页 Home（默认 Tab）
│   ├── 标题栏（FlowGallery + 搜索 + 网格切换）
│   ├── 文件夹标签栏（All / Folder1 / Folder1/Sub1 ... 含计数徽章）
│   ├── 统计栏（图片数 / 文件夹数 / HD 数）
│   ├── 瀑布流网格（双列，递归聚合）
│   ├── FAB（+ 添加文件夹）
│   └── 空状态（未添加文件夹时）
├── 搜索 Search
│   ├── 搜索框
│   └── 搜索结果瀑布流 / 空提示
├── 设置 Settings
│   ├── 文件夹管理（开关/添加/移出）
│   └── 显示设置（画质 / 清除缓存 / 关于）
└── 全屏查看器（全局覆盖层）
    ├── 顶部操作栏（返回 + 收藏 + 分享 + 更多）
    ├── 主图（缩放/平移/滑动导航）
    ├── 左右导航箭头
    └── 底部信息（文件名 + 尺寸 + 缩略图条）
```

---

## 5. 功能需求

### FR-1 瀑布流网格（P0）

**描述**：Pinterest 风格双列瀑布流，图片按真实宽高比自适应高度。

**需求细节**：
- 双列布局，图片按"最短列优先"算法分配（true masonry）
- 图片卡片圆角 12px，保留原始宽高比（非裁剪）
- 卡片加载时显示同比例占位（防跳动）
- 卡片下方渐变压暗层（底部 → 透明）
- 滚动使用懒加载，仅渲染可视区域

**交互**：
- 点击卡片 → 打开全屏查看器
- 卡片按压反馈：scale(0.98)

**验收标准**：
- 混合比例图片（竖图/横图/方图）下两列高度差 ≤ 一张卡片高度
- 快速滚动 500 张图无卡顿、无白屏闪烁

### FR-2 文件夹管理（P0）

**描述**：通过系统文件夹选择器（SAF）添加多个图包文件夹，支持开关、移出与聚合。

**需求细节**：
- **添加**：FAB 或设置页 → 系统 SAF 目录选择器 → 授权后加入图库
- **去重**：同一目录重复添加自动忽略
- **持久化**：目录授权与列表跨重启保留
- **开关**：设置页/模态框中可临时停用某文件夹（不删除授权）
- **移出**：将文件夹从图库中移除——删除列表条目并释放 SAF 持久化授权（`releasePersistableUriPermission`），其图片从瀑布流与统计中同步消失；移出前弹出确认对话框（提示图片数量），防止误操作

**交互**：
- 文件夹标签栏：`All [总数]` + 各文件夹 `[名称] [数量]`，激活态紫色高亮
- 点击标签 → 过滤瀑布流为该文件夹内容
- 文件夹模态框：底部弹出（Bottom Sheet），带拖动把手，多选勾选 + "Add New Folder" 按钮
- 移出入口：设置页文件夹条目左滑露出"移出"操作，或长按条目弹出菜单（移出/取消）；模态框中提供"移除"入口

**验收标准**：
- 添加 3 个文件夹后重启应用，文件夹列表与授权保持
- 开关关闭某文件夹后，首页统计与瀑布流即时更新
- 重复添加同一文件夹不产生重复条目
- 移出某文件夹后：列表条目消失、系统授权释放、瀑布流与统计即时更新；重启后不再出现
- 移出时弹出确认对话框，取消不产生任何变更

### FR-2.1 子文件夹递归浏览（P0）

**描述**：文件夹若包含子文件夹，按一级子文件夹维度递归浏览其下所有图片（含孙子层级的图片，即"图包套图包"场景）。

**需求细节**：
- **递归范围**：以用户添加的文件夹为根，向下递归所有层级子文件夹，收集全部图片（含子文件夹、孙文件夹……中的图片），聚合到该文件夹的瀑布流中
- **标签展示**：文件夹标签栏中，该文件夹的总计数 = 根目录 + 所有层级子目录的图片数之和
- **子文件夹独立标签**：若用户添加的文件夹包含一级子文件夹，标签栏在父文件夹之后以 `父文件夹/子文件夹` 形式展示每个一级子文件夹（含各自计数），点击可单独浏览该子文件夹递归范围内的图片；展开/折叠由标签栏横滑承载
- **面包屑感知**：进入子文件夹视图时，顶部可显示层级路径（如 `Landscapes > Sunset Pack`），点击父级路径返回
- **重复归属**：同一图片只归属其所在的实际文件夹标签；"All" 聚合视图按图片去重，不重复计数
- **性能**：递归扫描在 IO 线程异步执行；单文件夹超过 N 张（建议 5000）时按目录分批加载，滚动到接近底部时继续加载

**交互**：
- 首页标签栏出现子文件夹标签后，点击即切换到该子文件夹的瀑布流
- 当前处于子文件夹时，统计栏的图片数显示该子文件夹递归总数

**验收标准**：
- 添加含 2 层嵌套子文件夹的根目录后：父标签计数 = 全部层级图片总数；子文件夹标签计数 = 该子文件夹递归总数
- 点击任一子文件夹标签，瀑布流仅显示该子文件夹递归范围内的图片
- "All" 视图图片无重复（同一图片不出现两次）
- 深层嵌套（≥3 层）扫描不崩溃、不卡死主线程，滚动加载流畅
- 子文件夹标签可正常横滑访问（文件夹数量多时）

### FR-3 全屏查看器（P0）

**描述**：全屏沉浸式图片查看器，支持缩放、平移、滑动导航、缩略图跳转。

**需求细节**：
- **缩放**：双指捏合缩放 1x–4x，超过 1x 时可平移（拖动）
- **滑动**：缩放 1x 时左右滑动切换上一张/下一张（阈值 >50px）
- **导航**：左右半透明圆形箭头按钮（首/末张对应隐藏）；点击画面切换工具栏显隐
- **缩略图条**：底部横排当前图集缩略图，当前项白色描边高亮，点击跳转
- **信息**：底部显示文件名 + 尺寸（宽 × 高）
- **操作栏**：顶部返回、收藏、分享、更多
- **沉浸**：进入查看器隐藏系统状态栏/导航栏，退出恢复

**验收标准**：
- 缩放平滑无跳变，1x→4x 无极变速
- 放大状态下拖动不触发翻页
- 缩略图条在 100+ 张图集中滚动定位正常

### FR-4 文件夹标签与统计（P1）

**描述**：首页提供文件夹维度筛选与图库概览统计。

**需求细节**：
- 标签栏横滑（多文件夹时），含计数徽章
- 统计栏三项：`图片数` / `文件夹数` / `HD 数`
- 图片数随当前标签联动（All=总数，单文件夹=该夹数量）

### FR-5 质量标识 HD/SD（P1）

**描述**：缩略图左下角显示分辨率质量徽章。

**规则**：
- 宽 ≥1920 或 高 ≥1080 → HD（绿色徽章）
- 其余 → SD（橙色徽章）
- 徽章样式：圆角小标签，等宽字体，位于卡片左下角

### FR-6 收藏（P1）

**描述**：卡片与查看器均可收藏/取消收藏图片。

**需求细节**：
- 卡片右上角心形按钮（28px 圆形半透明底），已收藏为红色实心
- 查看器顶部心形同步状态
- 收藏状态跨会话保留

### FR-7 搜索（P2）

**描述**：按文件名模糊搜索当前图库。

**需求细节**：
- 搜索页独立 Tab，输入即时过滤
- 结果显示为瀑布流，可点击进入查看器
- 空查询显示引导文案；无结果显示"没有结果"

### FR-8 设置（P2）

**描述**：图库管理与显示偏好。

**需求细节**：
- 文件夹列表：每个文件夹显示名称 + 图片数 + 开关（启用/停用）
- 添加文件夹入口
- 图片质量：HD 缩略图开关
- 清除缓存：释放缩略图缓存（显示缓存量）
- 关于：应用名 + 版本号

### FR-9 下拉刷新（P2）

**描述**：瀑布流顶部下拉刷新，重新扫描文件夹内容（新增/删除图片同步）。

**需求细节**：
- 下拉出现旋转图标 + "Pull to refresh" 提示
- 下拉超过阈值触发刷新，刷新中显示 "Refreshing..."
- 刷新完成自动回到原位

---

## 6. 非功能需求

### NFR-1 性能

| 指标 | 要求 |
|---|---|
| 冷启动 | ≤ 2s（1000 张图场景） |
| 瀑布流滚动帧率 | ≥ 55fps（500 张缩略图在屏） |
| 缩略图加载 | 首屏 20 张 ≤ 1.5s |
| 全屏图加载 | ≤ 1s（10MB 以内 JPEG） |
| 内存占用 | 常驻 ≤ 150MB（1000 张图库） |

### NFR-2 权限与隐私

- 采用 **SAF（Storage Access Framework）** 目录选择，无需申请存储读写权限
- 目录授权经 `takePersistableUriPermission` 持久化，用户可随时在系统设置撤销
- 不上传任何图片数据；收藏/文件夹列表仅存本地

### NFR-3 兼容性

- minSdk 26（Android 8.0），targetSdk 最新稳定版
- 适配全面屏手势导航、刘海屏 inset
- 支持横竖屏（竖屏优先）

### NFR-4 稳定性

- 扫描超大文件夹（1 万张）不崩溃，异步分页处理
- 损坏图片文件跳过不阻塞
- 应用进入后台/恢复不丢失浏览位置

---

## 7. 视觉规范（依据设计稿浏览器实测分析）

> 本节依据 `flowgallery-android.html` 在 Chromium 中的实际渲染逐界面分析提取。

### 7.1 设计令牌

| Token | 值 | 用途 |
|---|---|---|
| 背景 bg | `#0A0A0C` | 全局底色 |
| 表面 surface | `#141418` | 导航栏/统计栏/卡片容器 |
| 表面-2 | `#1C1C22` | 图标底/次级容器 |
| 表面-3 | `#252530` | 计数徽章底 |
| 强调色 accent | `#7C5CFF` | 激活态/主按钮/FAB/选中导航 |
| 主文字 fg | `#F0F0F5` | 标题/正文 |
| 次级文字 | `#A0A0B0` | 描述/次要信息 |
| 弱文字 muted | `#606070` | 标签/占位 |
| 边框 border | `#2A2A35` | 分隔 |
| 成功/HD | `#22C55E` | HD 徽章 |
| 警告/SD | `#F59E0B` | SD 徽章 |
| 收藏红 | `#EF4444` | 收藏态 |

### 7.2 页面级规范（实测）

**首页 Home**
- 三段式：顶部信息区（标题+标签+统计）/ 中部瀑布流 / 底部导航
- 标题栏：左"FlowGallery"标题，右侧两个 40×40 圆形图标按钮（搜索、网格切换），背景 `#0A0A0C`
- 标签栏：横滑胶囊标签（圆角 20px），激活态紫色背景+白字+紫色计数徽章；未激活深灰背景+白字
- 统计栏：深灰圆角 12px 条，三项 `[图标 16px 紫] 数字(14px 粗) + 标签(9px 大写)`：16 IMAGES / 3 FOLDERS / HD QUALITY
- 瀑布流：双列间距 10px，卡片圆角 12px，按宽高比自适应，加载时同比例占位
- 卡片元素：左下角 HD(绿)/SD(橙) 圆角徽章（等宽字体 9px），右上角 28px 圆形半透明底心形
- FAB：56×56 圆角 16px 紫色，白色 + 号，距右下 20px，悬浮于导航栏上方 96px
- 底部导航：80px 高，图标 24px + 文字 11px，选中紫色，未选中 `#606070`

**文件夹模态框 Folder Modal**
- 底部弹出（slide-up 0.3s + 背景淡入 0.2s），背景模糊遮罩 `rgba(0,0,0,0.7)`
- 顶部圆角 24px，含 40×4px 拖动把手（圆角 2px，`#2A2A35`）
- 标题 "Select Folders"（20px 粗体）
- 文件夹条目：48×48 圆角 8px 图标容器（紫色文件夹图标）+ 名称(15px 粗) + 路径(12px 灰) + 数量；右侧 24px 圆形勾选（选中紫色底白勾）
- 选中条目：紫色边框 2px + 紫色 15% 背景；未选中深灰背景
- "Add New Folder"：全宽 16px 高，紫色 15% 背景 + 2px 紫色虚线边框，居中 + 图标 + 文字，悬停实心紫底白字

**全屏查看器 Viewer**
- 纯黑背景全屏，顶部操作栏（返回/收藏/分享/更多 44px 圆形半透明白底），主图 contain 居中
- 左右导航箭头：56px 圆形 `rgba(255,255,255,0.15)` 半透明，距边 12px，首/末张隐藏
- 缩放：1x–4x 滚轮/捏合，放大可平移；底部缩略图条 48×48 圆角 6px，当前项白边 2px
- 底部信息：文件名(14px 白 500) + 尺寸(12px 等宽 白 60%)，上覆黑色渐变 `rgba(0,0,0,0.8)→transparent`
- 缩放时工具栏保持可见（实测确认）

**搜索页 Search**
- 标题 "Search"（20px 粗体），搜索框圆角 12px 深灰底 `#141418`，内边距 12×16，左侧放大镜 20px 灰 + 占位符 "Search images..."
- 空状态：80×80 圆角 24px 图标容器 + 标题(18px 粗) + 描述(14px 灰)
- 搜索结果复用瀑布流

**设置页 Settings**
- 标题 "Settings"，分组标题大写 12px 灰（DISPLAY / ABOUT），间距 12px 上下
- 设置条目：圆角 12px 深灰卡片，40×40 圆角 8px 图标容器(浅灰图标) + 名称(15px 白) + 描述(12px 灰)
- 右侧元素：开关（48×28 圆角 14px，开启紫底白点滑 20px）/ 右箭头（20px 灰）
- 关于：FlowGallery + Version 1.0.0

### 7.3 字体

- 主字体：Inter（400/500/600/700）
- 等宽字体：JetBrains Mono（500）— 用于 HD/SD 徽章、尺寸信息

### 7.4 动效

- 模态框：背景淡入 0.2s + 内容上滑 0.3s
- 卡片按压：scale(0.98) 0.3s
- 导航切换：图标/文字颜色 0.2s 渐变
- 刷新指示：旋转动画 1s/圈

---

## 8. 技术需求（建议）

> 本节为实现建议，供技术评审参考，不作为 UI 约束。

| 项 | 建议 |
|---|---|
| 语言 | Kotlin |
| UI | Jetpack Compose + Material 3 |
| 状态管理 | ViewModel + StateFlow |
| 图片加载 | Coil（内存/磁盘双级缓存） |
| 持久化 | 文件夹列表：SharedPreferences/DataStore；收藏：轻量数据库 |
| 文件访问 | SAF（OpenDocumentTree + 持久化授权） |
| 元数据 | BitmapFactory bounds 解码获取宽高（SAF URI 不适用 MediaStore 列） |
| 瀑布流 | LazyVerticalStaggeredGrid（双列） |
| 并发 | Coroutines（IO 调度扫描、主线程渲染） |

---

## 9. 数据模型（草案）

```
Folder
├── id: Long
├── name: String          # 文件夹名（根目录显示名）
├── uri: String           # SAF 树 URI
├── imageCount: Int       # 缓存统计（递归总数：根+全部子层级）
├── isSelected: Boolean   # 是否启用
└── subFolders: List<SubFolder>   # 一级子文件夹（递归浏览标签）
    └── SubFolder
        ├── id: Long
        ├── name: String          # 子文件夹名
        ├── uri: String           # 子文件夹文档 URI
        └── imageCount: Int       # 该子文件夹递归总数

ImageItem
├── id: Long
├── folderId: Long        # 所属根文件夹
├── folderName: String
├── subFolderId: Long?    # 所属一级子文件夹（null=根目录直接图片）
├── subFolderName: String?
├── name: String          # 文件名
├── uri: String           # 文档 URI
├── width: Int            # 真实宽度
├── height: Int           # 真实高度
├── isFavorite: Boolean
└── isHd: Boolean(派生)   # w≥1920 || h≥1080
```

---

## 10. 验收总览

| 功能 | 优先级 | 验收要点 |
|---|---|---|
| 瀑布流 | P0 | 双列最短列分配、比例正确、滚动流畅 |
| 文件夹管理 | P0 | 多选添加、持久化、开关即时生效 |
| 文件夹移出 | P0 | 移除+授权释放+即时更新+确认对话框 |
| 子文件夹递归 | P0 | 多层级聚合、子文件夹标签、All 去重 |
| 全屏查看器 | P0 | 缩放 1–4x、滑动导航、缩略图跳转、沉浸模式 |
| 标签/统计 | P1 | 计数准确（含递归计数）、联动正确 |
| HD/SD 标识 | P1 | 判定规则正确 |
| 收藏 | P1 | 双端同步、持久化 |
| 搜索 | P2 | 即时过滤 |
| 设置 | P2 | 开关/缓存/关于 |
| 下拉刷新 | P2 | 扫描同步 |

---

## 11. 里程碑建议

| 阶段 | 范围 | 产出 |
|---|---|---|
| M1（P0） | 瀑布流 + 文件夹管理 + 全屏查看器 | 可体验核心闭环的测试版 |
| M2（P1） | 标签统计 + HD/SD + 收藏 | Beta 版 |
| M3（P2） | 搜索 + 设置 + 下拉刷新 + 打磨 | 正式版 1.0 |

---

## 12. 开放问题（待评审确认）

1. 是否需要**多列自适应**（横屏 3 列 / 平板 4 列）？设计稿仅含双列。
2. 收藏是否需独立"收藏夹"视图？设计稿仅提供收藏状态，无聚合页。
3. "网格切换"按钮（标题栏右侧）具体行为？（网格密度 2↔3 列，还是列表视图？）
4. 分享按钮落地方式：系统分享面板？保存到相册？
5. 是否需要视频/动图（GIF/WebP 动画）支持？设计稿仅覆盖静态图。

---

*文档结束 — 依据 `flowgallery-android.html` 设计稿生成，待评审后进入开发。*
