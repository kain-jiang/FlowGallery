package com.flowgallery.app.ui.theme

import androidx.compose.ui.graphics.Color

// FlowGallery dark palette — matches the design prototype (#0a0a0c + #7c5cff)
// Raised tones so text stays readable on OLED screens at low brightness.
val Bg = Color(0xFF17171D)           // app background — lifted from pure black
val Surface = Color(0xFF23232C)      // card surface
val Surface2 = Color(0xFF2D2D38)     // nested surface / icon chips
val Surface3 = Color(0xFF3A3A47)     // raised chips
val Fg = Color(0xFFFFFFFF)           // primary text — pure white
val FgSecondary = Color(0xFFD0D0DE)  // secondary text
val Muted = Color(0xFF9C9CAD)        // tertiary text
val Border = Color(0xFF44444F)
val Accent = Color(0xFF8B6DFF)       // slightly brighter accent for dark bg
val AccentHover = Color(0xFF9E85FF)
val AccentMuted = Color(0x338B6DFF)
val Success = Color(0xFF34D399)
val Warning = Color(0xFFFBBF24)
val Error = Color(0xFFF87171)

val LightBg = Color(0xFFF7F7FA)
val LightSurface = Color(0xFFFFFFFF)
val LightAccent = Color(0xFF6C4DF0)
